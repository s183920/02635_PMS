#include <stdlib.h>
#include <math.h>
#include "matrix.h"

int fwdsub(unsigned long n, double alpha, double **R, double *b);

int tri_sylvester_solve(const matrix_t *R, matrix_t *C) {
  if (R == NULL || C == NULL){return -2;}

  
  

}
